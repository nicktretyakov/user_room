# Set to true to run without database connection for development
SKIP_DATABASE=true

# Database configuration (only used when SKIP_DATABASE=false)
DATABASE_URL=postgres://user:password@localhost/auth_db
JWT_SECRET=your-secret-key
REDIS_URL=redis://localhost/
